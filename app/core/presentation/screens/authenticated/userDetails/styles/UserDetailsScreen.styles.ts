import {StyleSheet} from "react-native";
import {AppColors} from "../../../../../../theme/AppColors";

export const getUserDetailsScreenStyles = (colors: AppColors) => {
  return StyleSheet.create({
    bodyContainer: {
      margin: 16,
    },
  });
};
